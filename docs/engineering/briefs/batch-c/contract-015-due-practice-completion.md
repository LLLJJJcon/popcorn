# CONTRACT-015 atomic due Practice completion brief

- Requested by: Batch C Task 2 dependency audit.
- Baseline commit: `8d4ec2c`.
- Worktree/branch: `/private/tmp/popcorn-youtube-learning`, `codex/popcorn-youtube-learning`; controller-owned implementation.
- Plan/task: prerequisite for Batch C Task 2 and the reviewed repair of Batch C Task 3.
- Allowed modifications: one new migration `supabase/migrations/202608160014_due_practice_completion.sql`, one focused pgTAP `supabase/tests/due_practice_completion.sql`, `src/types/database.generated.ts`, this brief, `docs/engineering/handoffs/batch-c/contract-015-due-practice-completion.md`, and after acceptance `docs/engineering/execution-ledger.md`.
- Forbidden modifications: feature/domain/repository/route/UI code, existing migrations/tests, root config, lockfile, Provider/gateway/extension.
- Consumed interfaces: `practice_tasks`, canonical `attempts`, append-only `mastery_events`, `review_tasks`, immutable gateway provenance, frozen `advanceMastery` and `scheduleReview` semantics.
- Produced schema: explicit review→due task→attempt lifecycle for tasks completed through the RPC; `review_tasks.completed_at/completed_attempt_id`; optional due-task review link for compatibility with pre-existing unmaterialized due tasks, with the RPC requiring the link; deterministic context fingerprint and duplicate-context fence; one pending review per expression; private immutable completion receipt visible only inside the definer function. The locked review plus receipt, rather than global attempt/event indexes, guarantees one canonical attempt and mastery event for an atomic due completion without masking existing owner/provenance foreign-key validation.
- Produced RPC: service-role-only `public.complete_due_practice(...)` atomically locks an owned due review/task/expression, records one evaluated attempt, derives independent use from passed+assistance, advances only `tried→reused→owned` from persisted evidence, appends an evidence event, completes the old review, schedules one next review, and returns stable IDs/state/schedule with `created` replay status.
- Transaction rules: exact request-key replay is idempotent; different-key double completion, future/cancelled/stale/cross-owner/mismatched graph calls fail; Provider work happens before but never inside the RPC; client/AI cannot submit mastery, independent-use, due date, or interval. SQL is authoritative under row locks. Task 2 must compare returned evidence/state/schedule with the frozen TypeScript pure rules but cannot decide writes from transaction-external counts.
- Mastery/schedule: failed or assisted evidence never advances and retries in 1 day; successful independent due evidence moves `tried→reused` and schedules 7 days; `reused→owned` requires at least two distinct context fingerprints on at least two UTC dates and due-practice evidence, then schedules 30 days; `owned` is absorbing, with failed/assisted evidence retrying in 1 day and successful maintenance in 30 days.
- RED expectation: function/schema absent; pgTAP covers grants/private receipt, owner/stale/future/cancelled/mismatched graph, exact replay/different-key rejection, one task/attempt/event/pending review, failed/hint/model-answer schedules, tried→reused, reused threshold counterexamples and success, UTC boundary, owned absorption, immutable provenance, and late-failure rollback. Add a two-session concurrency proof if pgTAP cannot establish the race.
- GREEN/verification: clean reset migrations 001–014, focused pgTAP, full pgTAP, generated types exact, frozen mastery/schedule parity fixtures in SQL expectations, TypeScript, diff-check. No browser/build/application full suite unless a concrete shared-contract regression requires it.
- Upstream/license: no new upstream code. Preserve pinned YouTube Digest MIT adaptation `d03e1f61e017b032159ffd1821cac6e7693ce0c7`; LLM Wiki `723e259309aea5e3850265b631f80224f66dd9f6` remains method-only with no GPLv3 code/tests/prompts/components/assets copied.
